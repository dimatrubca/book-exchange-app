import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(() => ({
  root: {
    display: "flex",
    justifyContent: "flex-end",
    marginBottom: "20px",
  },

  linkItem: {
    marginLeft: "1rem",
  },
}));

export { useStyles };
