import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(() => ({
  root: {
    display: "flex",
    justifyContent: "flex-end",
  },

  linkItem: {
    marginLeft: "1rem",
  },
}));

export { useStyles };
