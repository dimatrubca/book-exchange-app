import React from "react";
import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  root: {
    maxWidth: 345,
  },
  cardContent: {
    height: 140,
    overflow: "hidden",
  },
  media: {
    height: 200,
  },
});
export { useStyles };
